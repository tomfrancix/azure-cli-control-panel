<Window x:Class="AzureCliControlPanel.App.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="Azure CLI Control Panel" Height="820" Width="1300"
        WindowStartupLocation="CenterScreen">
  <DockPanel>
    <StatusBar DockPanel.Dock="Bottom">
      <StatusBarItem>
        <TextBlock Text="{Binding StatusText}"/>
      </StatusBarItem>
      <Separator/>
      <StatusBarItem>
        <TextBlock Text="{Binding AzPathText}"/>
      </StatusBarItem>
      <Separator/>
      <StatusBarItem>
        <TextBlock Text="{Binding LastRefreshText}"/>
      </StatusBarItem>
    </StatusBar>

    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="*"/>
        <RowDefinition Height="210"/>
      </Grid.RowDefinitions>

      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="360"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>

      <Border Grid.Row="0" Grid.Column="0" BorderBrush="#33000000" BorderThickness="0,0,1,0">
        <DockPanel>
          <StackPanel DockPanel.Dock="Top" Orientation="Vertical" Margin="8">
            <TextBlock FontSize="16" FontWeight="SemiBold" Text="Authentication"/>
            <StackPanel Orientation="Horizontal">
              <Button Content="Login" Command="{Binding LoginCommand}" IsEnabled="{Binding IsNotBusy}"/>
              <Button Content="Logout" Command="{Binding LogoutCommand}" IsEnabled="{Binding IsNotBusy}"/>
              <Button Content="Refresh" Command="{Binding RefreshAllCommand}" IsEnabled="{Binding IsNotBusy}"/>
            </StackPanel>
            <TextBlock Text="{Binding SignedInText}" Margin="4,8,4,0"/>
          </StackPanel>

          <TabControl Margin="8">
            <TabItem Header="Subscriptions">
              <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
                  <Button Content="Reload" Command="{Binding LoadSubscriptionsCommand}" IsEnabled="{Binding IsNotBusy}"/>
                </StackPanel>

                <ListView ItemsSource="{Binding Subscriptions}" SelectedItem="{Binding SelectedSubscription}">
                  <ListView.ItemTemplate>
                    <DataTemplate>
                      <Border BorderBrush="#22000000" BorderThickness="0,0,0,1" Padding="6">
                        <StackPanel>
                          <TextBlock FontWeight="SemiBold" Text="{Binding Name}"/>
                          <TextBlock FontSize="12" Text="{Binding Id}"/>
                          <StackPanel Orientation="Horizontal">
                            <TextBlock FontSize="12" Text="Tenant: "/>
                            <TextBlock FontSize="12" Text="{Binding TenantId}"/>
                          </StackPanel>
                          <StackPanel Orientation="Horizontal">
                            <TextBlock FontSize="12" Text="State: "/>
                            <TextBlock FontSize="12" Text="{Binding State}"/>
                            <TextBlock FontSize="12" Text="  "/>
                            <TextBlock FontSize="12" Text="{Binding ActiveBadge}"/>
                          </StackPanel>
                          <StackPanel Orientation="Horizontal">
                            <Button Content="Switch" Command="{Binding SwitchCommand}" IsEnabled="{Binding CanSwitch}"/>
                          </StackPanel>
                        </StackPanel>
                      </Border>
                    </DataTemplate>
                  </ListView.ItemTemplate>
                </ListView>
              </DockPanel>
            </TabItem>

            <TabItem Header="Resource Groups">
              <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
                  <Button Content="Reload" Command="{Binding LoadResourceGroupsCommand}" IsEnabled="{Binding IsNotBusy}"/>
                </StackPanel>

                <TreeView ItemsSource="{Binding ResourceGroups}" SelectedItemChanged="TreeView_SelectedItemChanged">
                  <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                      <TextBlock Text="{Binding DisplayName}"/>
                    </HierarchicalDataTemplate>
                  </TreeView.ItemTemplate>
                </TreeView>
              </DockPanel>
            </TabItem>
          </TabControl>
        </DockPanel>
      </Border>

      <DockPanel Grid.Row="0" Grid.Column="1" Margin="8">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" VerticalAlignment="Center">
          <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="4"/>
          <TextBox Width="260" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" />
          <TextBlock Text="Type:" VerticalAlignment="Center" Margin="12,4,4,4"/>
          <ComboBox Width="220" ItemsSource="{Binding TypeFilters}" SelectedItem="{Binding SelectedTypeFilter}" />
          <TextBlock Text="RG:" VerticalAlignment="Center" Margin="12,4,4,4"/>
          <ComboBox Width="220" ItemsSource="{Binding ResourceGroupFilters}" SelectedItem="{Binding SelectedResourceGroupFilter}" />
          <Button Content="Reload Resources" Command="{Binding LoadResourcesCommand}" IsEnabled="{Binding IsNotBusy}" Margin="12,4,4,4"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding FilteredResources}" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  Margin="0,8,0,0">
          <DataGrid.Columns>
            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="220"/>
            <DataGridTextColumn Header="Type" Binding="{Binding DisplayType}" Width="170"/>
            <DataGridTextColumn Header="Resource Group" Binding="{Binding ResourceGroup}" Width="180"/>
            <DataGridTextColumn Header="State" Binding="{Binding StateText}" Width="90"/>
            <DataGridTextColumn Header="Tenant ID" Binding="{Binding IdentityTenantId}" Width="230"/>
            <DataGridTextColumn Header="Client ID" Binding="{Binding IdentityClientId}" Width="230"/>
            <DataGridTextColumn Header="Identity Source" Binding="{Binding IdentitySource}" Width="180"/>
            <DataGridTemplateColumn Header="Actions" Width="420">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <StackPanel Orientation="Horizontal">
                    <Button Content="Start" Command="{Binding StartCommand}" IsEnabled="{Binding CanStart}"/>
                    <Button Content="Stop" Command="{Binding StopCommand}" IsEnabled="{Binding CanStop}"/>
                    <Button Content="Restart" Command="{Binding RestartCommand}" IsEnabled="{Binding CanRestart}"/>
                    <Button Content="Logs" Command="{Binding LogsCommand}" IsEnabled="{Binding CanLogs}"/>
                    <Button Content="Log Config" Command="{Binding LogConfigCommand}" IsEnabled="{Binding CanLogConfig}"/>
                    <Button Content="Portal" Command="{Binding PortalCommand}" />
                    <Button Content="Kudu" Command="{Binding KuduCommand}" IsEnabled="{Binding CanKudu}"/>
                    <Button Content="Identity" Command="{Binding IdentityDetailsCommand}" />
                  </StackPanel>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
          </DataGrid.Columns>
        </DataGrid>
      </DockPanel>

      <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" BorderBrush="#33000000" BorderThickness="0,1,0,0">
        <DockPanel>
          <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="8">
            <TextBlock FontSize="14" FontWeight="SemiBold" Text="Output"/>
            <Button Content="Clear" Command="{Binding ClearOutputCommand}" Margin="12,0,0,0"/>
            <Button Content="Copy Selected" Command="{Binding CopySelectedOutputCommand}" Margin="8,0,0,0"/>
          </StackPanel>

          <DataGrid ItemsSource="{Binding OutputEntries}" SelectedItem="{Binding SelectedOutputEntry}"
                    AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                    HeadersVisibility="Column" GridLinesVisibility="Horizontal" Margin="8,0,8,8">
            <DataGrid.Columns>
              <DataGridTextColumn Header="Time" Binding="{Binding Timestamp}" Width="170"/>
              <DataGridTextColumn Header="Command" Binding="{Binding Command}" Width="420"/>
              <DataGridTextColumn Header="Exit" Binding="{Binding ExitCode}" Width="60"/>
              <DataGridTextColumn Header="Duration" Binding="{Binding Duration}" Width="90"/>
              <DataGridTextColumn Header="StdErr/StdOut (redacted)" Binding="{Binding Summary}" Width="*"/>
            </DataGrid.Columns>
          </DataGrid>
        </DockPanel>
      </Border>

    </Grid>
  </DockPanel>
</Window>
